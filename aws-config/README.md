# AWS Infrastrucutre configuration

This directory documents the aws infracture components used to build and secure the backend system.

The infrastructure was amnually configured to better understand, how individual aws services at a low level.

